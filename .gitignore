certs/
build_tools/

# mud prototype build artifacts
mud/orchestrator/mud-sandbox-orchestrator
mud/guest/*.elf
mud/guest/*.o

# root-level fly.toml is a deploy-time copy of fly/fly.toml (flyctl
# resolves the Containerfile path relative to -c's own directory,
# so a deploy from repo root needs a root-level copy) -- never committed.
/fly.toml
